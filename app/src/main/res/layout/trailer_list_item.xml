<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">

    <ImageView
        android:id="@+id/caret_icon"
        android:layout_width="@dimen/caret_icon"
        android:layout_height="@dimen/caret_icon"
        android:layout_marginLeft="@dimen/list_item_caret_icon_left_margin"
        app:layout_constraintBottom_toTopOf="@+id/guideline"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintTop_toTopOf="@+id/guideline"
        android:src="@drawable/ic_caret_right"/>

    <TextView
        android:id="@+id/trailer_list_link"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:paddingTop="10dp"
        android:paddingBottom="10dp"
        android:layout_marginLeft="@dimen/list_item_trailer_left_margin"
        android:layout_marginStart="@dimen/list_item_trailer_start_margin"
        app:layout_constraintLeft_toRightOf="@+id/caret_icon"/>

    <android.support.constraint.Guideline
        android:id="@+id/guideline"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        app:layout_constraintGuide_percent="0.5"/>
</android.support.constraint.ConstraintLayout>